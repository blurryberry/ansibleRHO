---

- hosts: all
  become: true
  tasks:

  - name: install apache2 and php packages for Raspberry Pi OS (Debian/Bullseye)
    package:
      name: 
        - "{{ apache_package }}"
        - "{{ php_package }}"
        - "{{ htop_package }}"
      state: latest
      update_cache: yes


